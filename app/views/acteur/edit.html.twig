{% extends 'base.html.twig' %}

{% block body %}
    <h1>Acteur edit</h1>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input type="submit" value="Edit" class="btn btn-warning"/>
    {{ form_end(edit_form) }}

            <a href="{{ path('acteur_index') }}"class="btn btn-info" style="    position: relative;
    top: -33px;
    left: 54px;">Back to the list</a>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete"class="btn btn-danger" style="position: relative;
    top: -66px;
    left: 174px;">
            {{ form_end(delete_form) }}
  
{% endblock %}
